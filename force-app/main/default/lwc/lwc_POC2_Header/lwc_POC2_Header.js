import { LightningElement, track, wire } from 'lwc';
import respoc from '@salesforce/resourceUrl/res_poc2';

export default class lwc_POC2_Header extends LightningElement {
    imagelogo = respoc + '/uta_logo.png';
}