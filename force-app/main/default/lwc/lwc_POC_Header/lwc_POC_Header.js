import { LightningElement } from 'lwc';
import respoc from '@salesforce/resourceUrl/res_poc';

export default class lwc_POC_Header extends LightningElement {
    imagelogo = respoc + '/uta_logo.png';
}